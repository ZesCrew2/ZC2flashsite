{
  "name": "zc2flashsite",
  "version": "0.0.0",
  "private": true,
  "description": "ZC2 Microsite — TypeScript sources compiled to zc2sitelol/assets/ts (gitignored).",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --mode development && vite build --watch --mode development & vite",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --write \"typescript/**/*.ts\"",
    "lint": "eslint \"typescript/**/*.ts\""
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0",
    "vite": "^7.3.6"
  }
}
